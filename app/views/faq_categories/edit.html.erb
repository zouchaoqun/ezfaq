<h2><%=l(:label_faq_category)%></h2>

<% labelled_tabular_form_for :category, @category, :url => { :action => 'edit', :id => @project, :category_id => @category } do |f| %>
<%= render :partial => 'faq_categories/form', :locals => { :f => f } %>
<%= submit_tag l(:button_save) %>
<% end %>
